/**
 * @author WMXPY
 * @namespace Icon
 * @description declare
 */

export type IconConfig = {

    display?: string;
    center?: boolean;
    circle?: boolean;
    thin?: boolean;
    larger?: boolean;
};
